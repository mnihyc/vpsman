# Large File Split Notes

The recommended source-file threshold is 1,000 lines. It is not a hard limit:
focused fixtures, integration tests, broad CLI/VTY parsers, and repository
models may exceed it when role separation is clear. Every source file above
the recommendation must be documented here with a reason and a next split
direction. `scripts/audit-large-files.sh` enforces that the current
recommendation set is documented.

## Current Justifications

| File | Current role | Split direction |
| --- | --- | --- |
| `./crates/agent/src/executor_tests.rs` | Agent executor integration tests covering command handling, files, terminal, process, and network behavior. | Move new operation-family tests into focused agent test modules instead of expanding this catch-all. |
| `./crates/agent/src/file_browser.rs` | Agent-side file browser implementation with listing, metadata, filtering, sorting, and safety checks. | Split path validation, directory listing, metadata shaping, and preview/read helpers when file-browser behavior changes next. |
| `./crates/agent/src/file_push.rs` | Agent-side file push/write implementation for inline and chunked payloads with atomic write policy. | Split payload decoding, atomic filesystem operations, and ownership/mode application helpers when upload behavior expands. |
| `./crates/agent/src/network_status.rs` | Agent runtime tunnel/kernel/Bird2 status collector and parser for the current topology evidence model. | Split kernel probes, Bird2 parsing, and adapter-health correlation if adding more runtime evidence. |
| `./crates/agent/src/runtime.rs` | Agent connection runtime, command execution coordination, telemetry, and gateway session loop. | Split gateway session management, command lifecycle, telemetry scheduling, and gateway session handling when touching runtime again. |
| `./crates/agent/src/terminal.rs` | Agent terminal lifecycle, PTY I/O, retention, idle timeout, bounded streaming, and command policy enforcement. | Split PTY lifecycle, stream retention, and command-policy validation if terminal behavior changes again. |
| `./crates/api/src/job_request.rs` | Central API-side job request validation and payload normalization for many privilege-gated operation families. | Move operation-family validators into focused modules as each operation changes, keeping only shared target/privilege/job fingerprint validation here. |
| `./crates/api/src/repository_ingest.rs` | Ingest repository for gateway telemetry, hello/session updates, and status transition persistence. | Split session lifecycle, telemetry rollups, and agent status history into focused repository modules. |
| `./crates/api/src/repository_inventory.rs` | Inventory repository read/write logic for agents, tags, aliases, and fleet views. | Split agent identity, tag/alias management, and fleet summary queries when inventory behavior changes. |
| `./crates/api/src/repository_jobs.rs` | In-memory and PostgreSQL job repository logic with target/output/retry-fingerprint and operation-specific extraction helpers. | Split target queue persistence, output persistence, job aggregation, and operation-metadata extraction into dedicated repository modules. |
| `./crates/api/src/repository_alert_notifications.rs` | Fleet alert notification-channel repository with CRUD, delivery history, statistics, retention, and audit metadata. | Split channel CRUD, delivery history queries, statistics aggregation, and retention helpers when alert delivery grows. |
| `./crates/api/src/repository_schedules.rs` | Schedule repository with fixed target snapshots, CRUD/search/sort, audit metadata, deferral, target update, and next-run calculations. | Split schedule CRUD, target-snapshot updates, audit/event metadata, and cron helpers as schedule workflows expand. |
| `./crates/api/src/repository_terminal_sessions.rs` | Terminal session repository with durable replay, retention, and PostgreSQL/in-memory implementations. | Split lifecycle, chunk retention, and replay query helpers when terminal streaming/backpressure work expands. |
| `./crates/api/src/repository_webhook_rules.rs` | Webhook rule repository with materialization, retry, retention, and event filtering logic. | Split rule CRUD, event materialization, delivery state, and retention queries as webhook adapters grow. |
| `./crates/api/src/routes_backups.rs` | Backup and restore HTTP route layer spanning request metadata, artifacts, restore plans, and policies. | Split backup requests, artifact upload/handoff, restore plans, and policy routes into focused modules. |
| `./crates/api/src/routes_dashboard.rs` | Dashboard aggregation route layer for fleet, telemetry, alerts, and grouped read models. | Split fleet summary, telemetry charts, alert summaries, and grouping helpers when dashboard behavior changes. |
| `./crates/api/src/tests.rs` | Broad API integration-style tests for core job, target, and repository behavior. | Move new feature-specific assertions into domain test modules and keep this file for cross-cutting job lifecycle coverage. |
| `./crates/api/src/tests_backups.rs` | Integration-style API tests for backup request metadata, encrypted artifacts, upload sessions, retained-output handoff, restore, rollback, and migration-adjacent backup behavior. | Split artifact upload/session tests, restore/rollback tests, and migration-link backup tests into focused `tests_backup_*` modules. |
| `./crates/api/src/tests_data_sources.rs` | Integration-style API tests for data-source preset lifecycle, assignment, hot-config rendering, source-status read models, and runtime object-store readiness. | Split preset lifecycle/hot-config tests from source-status/readiness tests into focused `tests_data_source_*` modules. |
| `./crates/api/src/tests_files.rs` | API tests for file browser, file transfer, multi-target file actions, and safety constraints. | Split browser, transfer, and multi-action tests when adding new file workflow behavior. |
| `./crates/common/src/expression.rs` | Shared selector/expression parser and matcher used by API, worker, and CLI target selection. | Split lexer, parser, evaluator, and tests into separate modules if expression grammar changes again. |
| `./crates/vpsctl/src/cli.rs` | Clap command schema for the full headless CLI vocabulary. | Move newly added command families into existing argument modules such as `cli_access.rs`, `cli_update.rs`, and future domain-specific CLI modules. |
| `./crates/vpsctl/src/commands_backups.rs` | CLI command helpers for backup policies, retention pruning, artifact upload/handoff, restore, and rollback workflows. | Split policy/prune commands, artifact upload/handoff commands, and restore/rollback helpers into focused modules. |
| `./crates/vpsctl/src/commands_file_transfer.rs` | CLI file-transfer orchestration for push/pull, multi-target policy, artifact upload, and output polling. | Split push, pull, artifact source, and multi-target wait/readback helpers when file-transfer behavior changes. |
| `./crates/vpsctl/src/commands_inventory.rs` | CLI inventory and preset read/write helpers that share target and query construction. | Split fleet alerts, telemetry, tag, and data-source preset command handlers into named modules as future changes touch them. |
| `./crates/vpsctl/src/vty_inventory.rs` | Interactive VTY inventory query parsing and API submission for many read-model commands. | Split fleet alerts, telemetry, tags, and data-source read models into separate VTY inventory modules. |
| `./crates/worker/src/main.rs` | Worker composition root for schedule materialization, status reconciliation, alerts, retention, and leases. | Split schedule materialization, agent-status reconciliation, and worker startup/configuration into focused modules. |
| `./crates/worker/src/webhook_rules.rs` | Worker-side webhook rule materialization, delivery, retry, partitioning, and retention. | Split delivery adapters, event selection/materialization, and retention pruning as webhook behavior expands. |
| `./frontend/src/panels/AccessPanel.tsx` | Access panel combining operators, direct agent identity import, key lifecycle, and gateway session workflows. | Split operator management, direct identity import, key lifecycle, and gateway session sections into subpanels. |
| `./frontend/src/panels/BackupsPanel.tsx` | Backup/restore panel covering requests, artifacts, policies, restore plans, and operator actions. | Split backup requests, artifacts, restore plans, and policy management into focused components. |
| `./frontend/src/panels/DashboardPanel.tsx` | Dashboard panel for fleet summary, alerts, telemetry groups, and operational overview cards. | Split summary cards, telemetry groups, and alert sections into focused components. |
| `./frontend/src/panels/DataSourcePresetPanel.tsx` | Data-source preset panel for preset lifecycle, assignments, status, and push actions. | Split preset lifecycle, assignment controls, status views, and push actions into focused components. |
| `./frontend/src/panels/FleetWorkspace.tsx` | Main fleet workspace composition with agent table, filters, bulk actions, details, and workspace state. | Split table controls, details drawer, bulk-action toolbar, and filter/state helpers into focused modules. |
| `./frontend/src/panels/JobDispatchPanel.tsx` | Job dispatch form and progress UI for many operation families and target selections. | Split operation-family editors, target selector, privilege prompt, and progress rendering into focused components. |
| `./frontend/src/panels/JobHistoryPanel.tsx` | Job history panel with filtering, target/output readback, bundles, and comparison actions. | Split filters, target/output viewers, artifact download, and comparison sections into subcomponents. |
| `./frontend/src/panels/SchedulesPanel.tsx` | Schedule CRUD panel with fixed-target previews, manual target-update workflow, due-run controls, and privilege confirmation. | Split schedule table, target snapshot editor, recurrence form, and action-confirmation helpers when schedule workflows change next. |
| `./frontend/src/panels/jobs/FileBrowserPanel.tsx` | File browser panel with listing, preview, transfers, and per-target file actions. | Split listing, preview, upload/download, and mutation controls into focused components. |
| `./frontend/src/panels/jobs/MultiFileActionsPanel.tsx` | Multi-target file action panel covering copy, rename, delete, chmod, chown, and mkdir workflows. | Split each action family and shared target/path validation into focused components. |
| `./frontend/src/styles/data.css` | Shared data/table/workspace styling for dense console views. | Split by panel or component family as styles are migrated to smaller modules. |
| `./frontend/src/styles/jobs.css` | Job dispatch/history/progress styling for dense operational job views. | Split dispatch, progress, history, and output styles into component-specific CSS files. |
| `./frontend/src/styles/shell.css` | Shell/terminal/console styling shared across interactive panels. | Split terminal, command form, and output styling into focused CSS files. |
| `./frontend/src/styles/workspace.css` | Main fleet workspace layout, responsive table, cards, and details styling. | Split workspace layout, agent table, filter bar, and details drawer styles as components are extracted. |
| `./frontend/src/types.ts` | Shared frontend API model aggregation that still anchors cross-panel type compatibility. | Continue moving domain-specific records into focused type modules as panels change. |
| `./frontend/tests/console-layout.spec.ts` | Broad desktop console workflow regression covering many panel actions with one shared mocked API surface. | Keep only cross-panel smoke flows here; move focused feature workflows into panel-specific specs when adding behavior. |
| `./frontend/tests/support/consoleLayoutFixtures.ts` | Shared mocked API dataset for console layout and workflow tests. | Split by domain fixtures once duplicate setup appears: fleet, jobs, topology, access, backups, and data sources. |
