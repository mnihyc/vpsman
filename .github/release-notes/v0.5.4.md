## Monitoring pipeline performance

- Monitoring cards now read their exact recent network window directly, removing broad history reconstruction without changing returned telemetry.
- Dashboard projection prepares ready fleet coordinates setwise while retaining independent per-VPS publication, reducing periodic PostgreSQL CPU bursts without delaying telemetry.
