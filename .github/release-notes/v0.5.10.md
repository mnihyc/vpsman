## OSPF bandwidth control

- Add a compact Dynamic toggle beside tunnel bandwidth, off by default. Static mode always uses configured bandwidth; Dynamic uses matching speed-test measurements within the existing ten-minute window, capped at the configured value.
- Measurement expiry, latency/loss probes, and agent runtime declarations are unchanged. No database migration is required.
