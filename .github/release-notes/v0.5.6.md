## Port-forwarding modes

- Add native REDIRECT and reusable custom adapters alongside DNAT, with mode-aware UI, CLI, and verified apply/removal lifecycle.
- Improve nftables compatibility for mapped ports and keep capability checks aligned with actual generated rules.
