Network tunnels now support additional IPv4/IPv6 addresses and automatic, stable per-tunnel IPv6 link-local management, with matching previews and CLI controls. Update agents to use these features.

Also fixes FOU tunnel creation and existing-device recognition. Primary monitoring targets and OSPF settings remain unchanged.
