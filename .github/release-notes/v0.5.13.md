Tunnel plans can now be renamed without changing evidence identity. Failed native tunnel creation cleans up only newly created resources; address prefixes and operator-edited drafts are handled correctly.

Multiline form fields now have compact, wrap-aware line numbers without changing input or argv behavior.

Upgrading resets tunnel measurements tied to the old evidence identities. Plans, credentials, and job history are preserved.
