Agent-owned tunnels now offer a compact Advanced editor with endpoint-specific command/config previews, OpenVPN tuning overrides, and templated lifecycle hooks.

Hooks follow native lifecycle ownership; each endpoint keeps separate settings, with execution limits grouped below the four commands. Existing plans retain their defaults without a database migration.
