## Fleet operation capacity

- Bulk fleet actions, including alert resolution, now consistently accept up to 1,000 selected records across the UI, API, and persisted schedules.
- Tests now verify observable behavior instead of source text; digest checks remain only where integrity is part of the product behavior.
