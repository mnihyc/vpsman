## Exact control-plane ownership

- Lifecycle transitions and UI projections now retain one exact owner across concurrent database, gateway, and browser work, preventing stale completion from reversing committed state.
- Smaller corrections restore network-rate inheritance, route-scoped refreshes, logical job-output grouping, alert/Ping/shared-view freshness, and normalized preference saves.
