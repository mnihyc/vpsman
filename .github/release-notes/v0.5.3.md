## Monitoring cards performance

- Selected network history now aggregates after per-interface counter validation, reducing cards query work without changing telemetry values or the API shape.
- Comfortable cards now render only VPS-local operational signals, restoring their intended layout without inferred page-level state.
